# ADR-008: 文件传输限定到受管目录

- 状态：Accepted
- 日期：2026-05-21

## 背景

历史接口允许客户端传入文件路径，存在路径穿越和任意文件覆盖风险。

## 决策

Phase -1 起上传、下载、重命名只接受相对路径，并解析到服务目录下的 `managed-files`。

## 影响

绝对路径和 `..` 被拒绝；后续如需扩展访问范围，必须通过 PAL PathResolver、RBAC 和审计。
